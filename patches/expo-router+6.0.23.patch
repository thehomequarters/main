diff --git a/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.h b/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.h
index 6bd933c..c6b68bc 100644
--- a/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.h
+++ b/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.h
@@ -1,7 +1,20 @@
 // Copyright 2015-present 650 Industries. All rights reserved.
 
+#if __has_include(<RNScreens/RNSDismissibleModalProtocol.h>)
 #import <RNScreens/RNSDismissibleModalProtocol.h>
+#else
+@protocol RNSDismissibleModalProtocol <NSObject>
+@optional
+@end
+#endif
+
+#if __has_include(<RNScreens/RNSTabBarController.h>)
 #import <RNScreens/RNSTabBarController.h>
+#else
+#import <UIKit/UIKit.h>
+@interface RNSTabBarController : UITabBarController
+@end
+#endif
 
 @interface LinkPreviewNativeNavigationObjC : NSObject
 
diff --git a/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.mm b/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.mm
index f20d576..141866b 100644
--- a/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.mm
+++ b/node_modules/expo-router/ios/LinkPreview/LinkPreviewNativeNavigation.mm
@@ -6,6 +6,33 @@
 #import <RNScreens/RNSScreen.h>
 #import <RNScreens/RNSScreenStack.h>
 
+// Compatibility stubs for react-native-screens versions that lack bottom-tabs
+// and per-screen ID APIs (e.g. react-native-screens < 4.x).
+#ifndef RNSBottomTabsScreenComponentView_defined
+#define RNSBottomTabsScreenComponentView_defined
+@interface RNSBottomTabsScreenComponentView : UIView
+@property (nonatomic, copy) NSString *tabKey;
+- (UIViewController *)reactViewController;
+@end
+#endif
+
+#ifndef RNSBottomTabsHostComponentView_defined
+#define RNSBottomTabsHostComponentView_defined
+@interface RNSBottomTabsHostComponentView : UIView
+- (UITabBarController *)controller;
+@end
+#endif
+
+// Category declarations so the compiler accepts screenIds / screenId access;
+// guarded at runtime with respondsToSelector: below.
+@interface RNSScreenStackView (ExpoRouterScreenIds)
+- (NSArray<NSString *> *)screenIds;
+@end
+
+@interface RNSScreenView (ExpoRouterScreenId)
+- (NSString *)screenId;
+@end
+
 @implementation LinkPreviewNativeNavigationObjC {
 }
 
@@ -73,7 +100,9 @@
 + (nonnull NSArray<NSString *> *)getStackViewScreenIds:(UIView *)view {
   if (view != nil && [view isKindOfClass:[RNSScreenStackView class]]) {
     RNSScreenStackView *stackView = (RNSScreenStackView *)view;
-    return stackView.screenIds;
+    if ([stackView respondsToSelector:@selector(screenIds)]) {
+      return stackView.screenIds;
+    }
   }
   return @[];
 }
@@ -89,7 +118,9 @@
 + (nonnull NSString *)getScreenId:(UIView *)view {
   if (view != nil && [view isKindOfClass:[RNSScreenView class]]) {
     RNSScreenView *screenView = (RNSScreenView *)view;
-    return screenView.screenId;
+    if ([screenView respondsToSelector:@selector(screenId)]) {
+      return screenView.screenId;
+    }
   }
   return nil;
 }
